apiVersion: v1
kind: Service
metadata:
  name: mtovxza
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mtovxza"
    port: 8080
  selector:
    app: mtovxza
